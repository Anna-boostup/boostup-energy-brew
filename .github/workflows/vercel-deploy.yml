name: Vercel Deploy

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  # Force Node 24 and suppress deprecation noise
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
  # Silence the Node 20 warning specifically
  NODE_OPTIONS: "--no-deprecation"

on:
  push:
    branches:
      - main
      - development
      - preview

permissions:
  contents: write

jobs:
  Test-and-Promote:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.7
        with:
          fetch-depth: 0
      
      - name: Setup Node.js 22
        uses: actions/setup-node@v4.0.3
        with:
          node-version: 22
          
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
        
      - name: Pull Vercel Env
        run: vercel pull --yes --environment=development --token=${{ secrets.VERCEL_TOKEN }}
        
      - name: Create .env
        run: |
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" >> .env
          echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env
        
      - name: Build
        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          
      - name: Deploy
        id: deploy
        run: |
          DEPLOYMENT_URL=$(vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }} | tail -n 1)
          if [ -z "$DEPLOYMENT_URL" ]; then echo "ERROR: Deployment failed"; exit 1; fi
          echo "DEPLOYMENT_URL=$DEPLOYMENT_URL" >> $GITHUB_ENV
          echo "url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT

      - name: Wait for 200 OK
        run: |
          echo "Waiting for $DEPLOYMENT_URL..."
          timeout 120s bash -c 'until curl -sI -L -H "x-vercel-protection-bypass: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}" "$DEPLOYMENT_URL" | grep "200" > /dev/null; do sleep 5; done'
          
      - name: Verify Health (Advanced Watchdog)
        run: |
          # Wait a bit for the 5s JS watchdog to trigger in the browser if it fails
          sleep 10
          BODY=$(curl -s -L -H "x-vercel-protection-bypass: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}" "$DEPLOYMENT_URL")
          
          if [[ "$BODY" == *"DIAGNOSTICKÝ PROTOKOL"* ]]; then
            echo "CRITICAL: Frontend Crash Caught!"
            echo "--- START ERROR DUMP ---"
            echo "$BODY"
            exit 1
          fi

          if [[ "$BODY" == *"Momentálně ladíme formy"* ]]; then
            echo "ERROR: Silent Hang Detected (Maintenance shell remains visible after timeout)."
            exit 1
          fi

          echo "SUCCESS: App is healthy. React mounted correctly."

      - name: Install & Test
        run: |
          npm install
          npx playwright install --with-deps
          npx playwright test tests/smoke.spec.ts tests/checkout_scenarios.spec.ts tests/admin.spec.ts
        env:
          PLAYWRIGHT_TEST_BASE_URL: ${{ env.DEPLOYMENT_URL }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
          TEST_ADMIN_EMAIL: ${{ secrets.TEST_ADMIN_EMAIL }}
          TEST_ADMIN_PASSWORD: ${{ secrets.TEST_ADMIN_PASSWORD }}

      - name: Upload Report
        if: failure()
        uses: actions/upload-artifact@v4.3.3
        with:
          name: playwright-report
          path: playwright-report/

      - name: Promote
        if: success()
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git checkout preview
          git merge development --no-ff -m "chore: auto-promote [skip ci]"
          git push origin preview

  Deploy-Preview:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/preview'
    steps:
      - uses: actions/checkout@v4.1.7
      - name: Setup Node
        uses: actions/setup-node@v4.0.3
        with:
          node-version: 22
      - run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
      - run: |
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" >> .env
          echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env
          vercel build --token=${{ secrets.VERCEL_TOKEN }}
      - run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}

  Deploy-Production:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4.1.7
      - name: Setup Node
        uses: actions/setup-node@v4.0.3
        with:
          node-version: 22
      - run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - run: |
          echo "VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}" >> .env
          echo "VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}" >> .env
          vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
